{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "FabLabs ${libname} version ${libver}",
  "icon": "assets/api-provider-api/icon.png",
  "description": "Template for starting out FabLabs drafts",
  "license": "MIT",
  "contact": {
    "sources": "https://github.com/${repo}"
  },
  "environment": "*",
  "depends": {
    "fabricloader": ">=0.4.0"
  },
  "custom": {
    "modmenu:api": true
  },
  "mixins": [
    "api-provider-api-v1.mixins.json"
  ]
}
